# Oracle Contracts

Customer overlays should depend on objects exposed through this layer instead of
reaching directly into `core/` internals. Add stable views and functions here,
then list them in `manifest.yaml`.
